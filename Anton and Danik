n=int(input(""))
s=str(input(""))

an = "A"
de=  "D"

finalan= [each for each in s if each in an]
x=len(finalan)

finalde= [each for each in s if each in de]
y=len(finalde)


if(x>y):
    print("Anton")
elif(y>x):
    print("Danik")
else:
    print("Friendship")
